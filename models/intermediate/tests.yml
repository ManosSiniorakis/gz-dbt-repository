version: 2

models:
  - name: int_orders_margin
    description: combinded margin and shipping
    columns:
      - name: orders_id
        description: the unique combination of model color size
        tests:
          - unique
  - name: int_sales_margin
    description: combines product and sales
    columns:
      - name: products_id
        description: the unique id of a product
        tests:
          - not_null
        
        
     
    